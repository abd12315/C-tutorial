#include <stdio.h>
int main()
{
    // this one explains the working of augemented assignmented operator in detail
    /*the = use to replace operator where operator takes the value of the arguments and return the value back as 
    the same variable X=X+1
    X+=1*/
    int x=10;
    //x=x+2;
    //x+=2;
    //x=x-3;
    // x-=3;
    // x=x*3;
    //x*=3;
    //x=x/3;
    //x/=3;

    // augemented assignment operator is same as the each operation describe individually of rach individual augemented operations//

    //let's test this theory out IRL?
    printf("Value:%d",x);
    return 0;
}